name: CI/CD

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
    branches: [main]
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: 'go.mod'
          cache: true
      - run: make test

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: 'go.mod'
          cache: true
      - uses: golangci/golangci-lint-action@v6
        with:
          version: v1.64.8
          install-mode: goinstall
          args: --timeout 5m

  docker:
    runs-on: ubuntu-latest
    needs: [test, lint]
    if: github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: read
      packages: write
    outputs:
      image-tag: ${{ fromJSON(steps.meta.outputs.json).tags[0] }}
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=branch
            type=semver,pattern={{version}}
            type=raw,value=latest,enable={{is_default_branch}}

      - uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy-staging:
    runs-on: ubuntu-latest
    needs: docker
    if: github.ref == 'refs/heads/main'
    environment: staging
    steps:
      - uses: actions/checkout@v4

      - name: Copy docker-compose.yml
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ vars.HOST }}
          username: ${{ vars.USER }}
          key: ${{ secrets.SSH_KEY }}
          port: ${{ vars.PORT }}
          source: docker-compose.yml
          target: /opt/kurut-bot

      - name: Deploy
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ vars.HOST }}
          username: ${{ vars.USER }}
          key: ${{ secrets.SSH_KEY }}
          port: ${{ vars.PORT }}
          envs: TELEGRAM_BOT_TOKEN,TELEGRAM_ADMIN_IDS,TELEGRAM_ASSISTANT_IDS,YOOKASSA_SHOP_ID,YOOKASSA_SECRET_KEY,YOOKASSA_MOCK_PAYMENT,YOOKASSA_RETURN_URL
          script: |
            set -e
            cd /opt/kurut-bot
            mkdir -p data

            echo "=== Debug: check env vars ==="
            echo "ADMIN_IDS length: ${#TELEGRAM_ADMIN_IDS}"
            echo "ASSISTANT_IDS length: ${#TELEGRAM_ASSISTANT_IDS}"

            echo "=== Login ==="
            echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

            echo "=== Pull ==="
            docker pull ${{ needs.docker.outputs.image-tag }}

            echo "=== Deploy ==="
            export IMAGE_TAG="${{ needs.docker.outputs.image-tag }}"
            export TELEGRAM_BOT_TOKEN="$TELEGRAM_BOT_TOKEN"
            export TELEGRAM_ADMIN_IDS="$TELEGRAM_ADMIN_IDS"
            export TELEGRAM_ASSISTANT_IDS="$TELEGRAM_ASSISTANT_IDS"
            export YOOKASSA_SHOP_ID="$YOOKASSA_SHOP_ID"
            export YOOKASSA_SECRET_KEY="$YOOKASSA_SECRET_KEY"
            export YOOKASSA_MOCK_PAYMENT="$YOOKASSA_MOCK_PAYMENT"
            export YOOKASSA_RETURN_URL="$YOOKASSA_RETURN_URL"

            docker compose up -d

            echo "=== Wait for container ==="
            sleep 5

            echo "=== Migrations ==="
            docker exec kurut-bot goose -dir /app/migrations sqlite3 /app/data/kurut.db up

            echo "=== Healthcheck ==="
            for i in $(seq 1 30); do
              STATUS=$(docker inspect --format='{{.State.Health.Status}}' kurut-bot 2>/dev/null || echo "starting")
              echo "[$i/30] $STATUS"
              [ "$STATUS" = "healthy" ] && echo "OK!" && docker image prune -f && exit 0
              [ "$STATUS" = "unhealthy" ] && docker logs kurut-bot --tail 30 && exit 1
              sleep 2
            done
            echo "Timeout!" && docker logs kurut-bot --tail 30 && exit 1
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_ADMIN_IDS: ${{ vars.TELEGRAM_ADMIN_IDS }}
          TELEGRAM_ASSISTANT_IDS: ${{ vars.TELEGRAM_ASSISTANT_IDS }}
          YOOKASSA_SHOP_ID: ${{ secrets.YOOKASSA_SHOP_ID }}
          YOOKASSA_SECRET_KEY: ${{ secrets.YOOKASSA_SECRET_KEY }}
          YOOKASSA_MOCK_PAYMENT: ${{ vars.YOOKASSA_MOCK_PAYMENT }}
          YOOKASSA_RETURN_URL: ${{ vars.YOOKASSA_RETURN_URL }}

  deploy-production:
    runs-on: ubuntu-latest
    needs: docker
    if: startsWith(github.ref, 'refs/tags/v')
    environment: production
    steps:
      - uses: actions/checkout@v4

      - name: Copy docker-compose.yml
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ vars.HOST }}
          username: ${{ vars.USER }}
          key: ${{ secrets.SSH_KEY }}
          port: ${{ vars.PORT }}
          source: docker-compose.yml
          target: /opt/kurut-bot

      - name: Deploy
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ vars.HOST }}
          username: ${{ vars.USER }}
          key: ${{ secrets.SSH_KEY }}
          port: ${{ vars.PORT }}
          envs: TELEGRAM_BOT_TOKEN,TELEGRAM_ADMIN_IDS,TELEGRAM_ASSISTANT_IDS,YOOKASSA_SHOP_ID,YOOKASSA_SECRET_KEY,YOOKASSA_RETURN_URL
          script: |
            set -e
            cd /opt/kurut-bot
            mkdir -p data

            echo "=== Login ==="
            echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

            echo "=== Pull ==="
            docker pull ${{ needs.docker.outputs.image-tag }}

            echo "=== Deploy ==="
            export IMAGE_TAG="${{ needs.docker.outputs.image-tag }}"
            export TELEGRAM_BOT_TOKEN="$TELEGRAM_BOT_TOKEN"
            export TELEGRAM_ADMIN_IDS="$TELEGRAM_ADMIN_IDS"
            export TELEGRAM_ASSISTANT_IDS="$TELEGRAM_ASSISTANT_IDS"
            export YOOKASSA_SHOP_ID="$YOOKASSA_SHOP_ID"
            export YOOKASSA_SECRET_KEY="$YOOKASSA_SECRET_KEY"
            export YOOKASSA_RETURN_URL="$YOOKASSA_RETURN_URL"

            docker compose up -d

            echo "=== Wait for container ==="
            sleep 5

            echo "=== Migrations ==="
            docker exec kurut-bot goose -dir /app/migrations sqlite3 /app/data/kurut.db up

            echo "=== Healthcheck ==="
            for i in $(seq 1 30); do
              STATUS=$(docker inspect --format='{{.State.Health.Status}}' kurut-bot 2>/dev/null || echo "starting")
              echo "[$i/30] $STATUS"
              [ "$STATUS" = "healthy" ] && echo "OK!" && docker image prune -f && exit 0
              [ "$STATUS" = "unhealthy" ] && docker logs kurut-bot --tail 30 && exit 1
              sleep 2
            done
            echo "Timeout!" && docker logs kurut-bot --tail 30 && exit 1
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_ADMIN_IDS: ${{ vars.TELEGRAM_ADMIN_IDS }}
          TELEGRAM_ASSISTANT_IDS: ${{ vars.TELEGRAM_ASSISTANT_IDS }}
          YOOKASSA_SHOP_ID: ${{ secrets.YOOKASSA_SHOP_ID }}
          YOOKASSA_SECRET_KEY: ${{ secrets.YOOKASSA_SECRET_KEY }}
          YOOKASSA_RETURN_URL: ${{ vars.YOOKASSA_RETURN_URL }}
